print("Welcome to the tip calculator!") #msg
bill = float(input("What was the total bill? $")) #bill amount as flt
tip = int(input("What percentage tip would you like to give? 10 12 15 ")) #tip int
people = int(input("How many people to split the bill? ")) #amount of ppl
bill_with_tip = (bill/people*(1+(tip/100))) #calculate bill+tip
bill_round = round(bill_with_tip,2) #round bill with tip
print("Each person should pay " + str(bill_round) + "$")
